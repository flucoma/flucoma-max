name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: curl SDK
      run: curl -L0 https://cycling74.s3.amazonaws.com/download/max-sdk-8.0.3.zip --output sdk.zip

    - name: unzip
      run: unzip sdk.zip
      
    - name: make build directory
      run: mkdir -p build
      
    - name: change to build directory
      run: cd build
      
    - name: cmake
      run: cmake -DMAX_SDK_PATH=../max-sdk-8.0.3/ -DCMAKE_OSX_ARCHITECTURES="x86_64"..
      
    - name: install
      run: make install
